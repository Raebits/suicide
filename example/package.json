{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./build",
    "start": "node ./build/index.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t raebits/docker-suicide-example:1.0 ."

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "suicide": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^4.6.3"
  }
}
